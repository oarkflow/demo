server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # Required for React/Vite
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Optional but recommended: serve gzip files if available
    gzip_static on;
}
